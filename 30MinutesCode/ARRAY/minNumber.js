/**
 * 返回数组中n个最小的元素 
 * 
 *  Array.slice()  获取指定元素的个数 
 * 
 *    先数组排序  按降序或升序排序 
 * 
 *  然后slice截取 
 *
 * @param {*} arr
 * @param {number} [n=1]
 */
const minNumber = function(arr, n = 1) {
   console.log('arr===',arr);
}


let n1 = [1,2,3,4,5,6,7]
console.log(minNumber(n1));