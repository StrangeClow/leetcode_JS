/**
 *   最长回文串
 * 
 *      给定一个包含大写字母和小写字母的字符串，找到通过这些字母构造成的最长的回文串，
 * 在构造过程中，请注意区分大小写，
 * 
 *   注意： 假设字符串的长度不会超过1010
 * 
 *   input==> abccccdd
 *   output ==> 7
 * 
 *   
 *
 * @param {*} str
 * @returns
 */
const longestPalindrome = function(str) {
    return res
}