/**
 *给你一个仅由数字 6 和 9 组成的正整数 num。
 
 你最多只能翻转一位数字，将 6 变成 9，或者把 9 变成 6 。
 
 请返回你可以得到的最大数字。
 *
 * @param {*} arr
 * @returns
 * 
 *  具体实例参见力扣中文版
 * 
 *  从首位开始 找到第一位出现的6的位置,然后替换为9 
 * 
 */
const maximum69Number = function(arr) {
    return maxNum
}