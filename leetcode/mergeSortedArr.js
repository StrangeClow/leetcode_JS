/**
 *    合并排序的数组 
 * 
 *        给定两个排序后的数组A和B，其中A的末端有足够的缓冲空间容纳B，编写一个方法，
 * 将B合并入A并排序
 * 
 * 
 * 初始化A和B的元素数量分别为m和n
 * 
 *   input  ===>   A [1,2,3,0,0,0],   m = 3
 *                 B [2,5,6],  m = 3
 * 
 *   output ===>  [1,2,2,3,5,6]
 * 
 *   说明 A.length = m + n
 *
 * @param {*} A
 * @param {*} m
 * @param {*} B
 * @param {*} n
 * @returns
 */
const mergeSortedArr = function(A, m, B, n) {
    return arr
}