
/**
 * @description: 替换所有的问号
 * @param {*}
 * @return {*}
 */
const modifyString = function(str) {
    let tempArr = str.split('')
     tempArr.forEach((item, index ,tempArr) => {
         if() {
             
         }
     })
}