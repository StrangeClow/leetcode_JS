/**
 *   礼物的最大价值  
 *
 * @param {*} grid
 * @returns
 */
const theGiftOfMaxValue = function(grid) {
    
    // 存在的边界情况 
    if(grid.length === 0 || grid[0].length === 0) {
        return 0
    }
    
    // 以某一值填充当前数据  
    let dp = []

    


}